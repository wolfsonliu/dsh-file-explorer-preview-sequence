import { SeqViz } from 'seqviz'
import seqparse from 'seqparse'

export const inject = ['fileExplorer']

export function apply(): void {
  // Spike: prove seqparse + seqviz bundle into lib/client.js. Replaced in Task 5.
  void SeqViz
  void seqparse
}
